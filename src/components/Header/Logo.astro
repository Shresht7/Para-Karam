<a href="/" id="logo-container" class="fly-in">
    <img id="logo" src="/favicon.svg" alt="logo" />
    <div id="name">Para-Karam</div>
</a>

<style>
    #logo-container {
        display: flex;
        flex-direction: row;
        justify-content: flex-start;
        align-items: center;
        gap: 1rem;
        --fly-from: 0 -100px;
        animation-fill-mode: both;
        animation-delay: 500ms;
        animation-duration: 500ms;
        animation-timing-function: ease-in-out;
    }

    #logo-container:hover {
        color: white;
        text-shadow: 0px 0px 1px white;
    }

    #logo {
        --size: 48px;
        width: var(--size);
        height: var(--size);
        aspect-ratio: 1 / 1;
    }

    #name {
        font-size: 2rem;
    }
</style>
