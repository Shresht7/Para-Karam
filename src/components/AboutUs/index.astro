---
// Props
interface Props {
    title?: string;
    showLink?: boolean;
}
const { title = "About Us", showLink = true } = Astro.props;
---

<section id="about-us">
    <div class="container flex flex-col">
        <h2 class="animate view-timeline">{title}</h2>
        <p class="animate view-timeline">
            We are <strong>Para-Karam</strong>, an organization dedicated to
            fostering a spirit of adventure and resilience amoung youth through
            exciting and safe adventure activities. We aim to boost adventure
            sports and create opportunities for adventure enthusiasts.
        </p>
        <p class="animate view-timeline">
            We are three retired officers of the Indian Air Force with extensive
            experience and expertise in various adventure activities including
            para-sailing, trekking, camping and other related pursuits. Our
            collective experience spans decades of service to the nation, where
            we honed our skill in rigorous and challenging environments. Now we
            wish to share our expertise with the young generation and broader
            community.
        </p>
        {
            showLink && (
                <a href="/our-team" class="flex animate view-timeline">
                    Read more about our Team <span class="arrow">→</span>
                </a>
            )
        }
    </div>
</section>

<style>
    section {
        padding-block: 8rem;
        background: var(--gradient-1);
        color: var(--color-white);
    }

    div {
        gap: 2rem;
        text-align: center;
        text-wrap: pretty;
        width: min(90dvw, 768px);
        animation-name: fade, scale;
    }

    a {
        gap: 0.5rem;
        border-bottom: 1px solid var(--color-white);
        transition: border-bottom var(--transition-duration);
    }

    a:hover {
        border-color: var(--color-blue);
    }

    .arrow {
        font-size: var(--font-size-xl);
    }

    .animate {
        animation-name: fade, fly, scale;
    }
</style>
